# ADR-003: Single Optional Runtime Semantic Reviewer

Status: Accepted

Production may bind one provider to the `VisualReviewer` interface. No multi-provider cascade or consensus is required by product architecture. Provider selection is benchmark-driven and replaceable without changing the reconstruction core.
