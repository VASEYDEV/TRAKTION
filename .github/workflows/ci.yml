name: ci

on:
  pull_request:
  push:
    branches: [main]

jobs:
  gate:
    name: repo-standard gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run verification gate
        run: bash scripts/gate.sh
