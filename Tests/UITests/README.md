# UI tests

The Milestone 0 SwiftUI preview shell has no automated interaction contract yet. UI tests begin with the native import/editor flow; reconstruction mathematics remains covered below the UI boundary.
