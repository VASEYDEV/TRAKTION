# Real-world fixtures

Private captures are never committed here. Sanitized fixtures require explicit approval and must not contain personal data, credentials, or private communications. Local/private fixture directories are ignored by Git.
